<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="szu.dao.UpdateDao">
    <resultMap id="BaseResultMap" type="szu.model.Update">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="uid" jdbcType="INTEGER" property="uid"/>
        <result column="content" jdbcType="VARCHAR" property="content"/>
        <result column="status" jdbcType="INTEGER" property="status"/>
        <result column="dateTime" jdbcType="TIMESTAMP" property="datetime"/>
        <result column="urls" jdbcType="VARCHAR" property="urls"/>
    </resultMap>
    <insert id="insert">
        insert into `update` (uid, content, status, dateTime, urls)
        values (#{uid,jdbcType=INTEGER}, #{content,jdbcType=VARCHAR}, #{status,jdbcType=INTEGER},
                #{datetime,jdbcType=TIMESTAMP}, #{urls,jdbcType=VARCHAR})
    </insert>
    <update id="update">
        update `update`
        set uid = #{uid,jdbcType=INTEGER},
            content = #{content,jdbcType=VARCHAR},
            status = #{status,jdbcType=INTEGER},
            dateTime = #{datetime,jdbcType=TIMESTAMP},
            urls = #{urls,jdbcType=VARCHAR}
        where id = #{id,jdbcType=INTEGER}
    </update>
    <delete id="deleteById">
        delete
        from `update`
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <delete id="deleteByVid">
        delete
        from `update`
        where uid = #{uid,jdbcType=INTEGER}
    </delete>
    <select id="findInPage" resultMap="BaseResultMap">
        select *
        from `update`
    </select>
    <select id="findByUid" resultMap="BaseResultMap">
        select *
        from `update`
        where uid = #{uid,jdbcType=INTEGER}
    </select>
    <select id="findById" resultMap="BaseResultMap">
        select *
        from `update`
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="findAll" resultType="szu.model.Update">
        select *
        from `update`
    </select>
    <select id="findByVid" resultType="szu.model.Update">
        select *
        from `update`
        where uid = #{uid,jdbcType=INTEGER}
    </select>

</mapper>