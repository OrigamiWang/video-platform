<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="szu.dao.PartitionDao">
  <resultMap id="BaseResultMap" type="szu.model.Partition">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
  </resultMap>

  <insert id="insert">
    insert into update_partition (name)
    values (#{name,jdbcType=VARCHAR})
  </insert>

  <update id="update">
    update update_partition
    <set>
      <if test="name != null">
        name = #{name,jdbcType=VARCHAR},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>

  <delete id="deleteById">
    delete from update_partition where id = #{id}
  </delete>

  <select id="findAll" resultMap="BaseResultMap">
    select * from update_partition
  </select>

  <select id="findById" resultMap="BaseResultMap">
    select * from update_partition where id = #{id}
  </select>

</mapper>